from . import my, events, prototypes, weekly_digests, announcements, feedbacks

mutations = [
    my.Mutation,
    events.Mutation,
    prototypes.Mutation,
    weekly_digests.Mutation,
    announcements.Mutation,
    feedbacks.Mutation,
]
