version: "2"

options:
  compose-file-path: 'docker/compose.dev-mac.yml'
syncs:
  data:
    src: '/data/kocherga'
  code:
    notify_terminal: true
    src: '.'
    sync_excludes:
      - 'node_modules'
      - '.git'
