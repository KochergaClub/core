apiVersion: v1
kind: Service
metadata:
  name: kocherga-redis
spec:
  ports:
  - port: 6379
  selector:
    app: kocherga-redis
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kocherga-redis
spec:
  selector:
    matchLabels: 
      app: kocherga-redis
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: kocherga-redis
    spec:
      containers:
        - name: redis
          image: redis:5.0
          ports:
            - containerPort: 6379
              name: redis
          resources:
            requests:
              memory: "4Mi"
            limits:
              memory: "1024Mi"
