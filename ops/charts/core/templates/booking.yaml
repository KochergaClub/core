apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-booking
  namespace: {{ .Release.Namespace }}
  labels:
    release: {{ .Release.Name }}
spec:
  ports:
  - port: 80
  selector:
    app: {{ .Release.Name }}-booking
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-booking
  namespace: {{ .Release.Namespace }}
  labels:
    release: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: {{ .Release.Name }}-booking
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-booking
        release: {{ .Release.Name }}
    spec:
      containers:
      - name: default
        image: {{ .Values.image.booking }}
        ports:
          - containerPort: 80
        resources:
          requests:
            memory: "8Mi"
          limits:
            memory: "256Mi"
