type Query {
  _empty: String

  rooms: [Room]!
}

type Mutation {
  _empty: String
}

type PageInfo {
  pageNumber: Int!  # supported until we move to cursor-based pagination entirely
  hasNextPage: Boolean!
}
