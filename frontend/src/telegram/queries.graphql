fragment TelegramChat on TelegramChat {
  id
  username
  title
  photo(spec: "fill-50x50") {
    id
    url
  }
  photo_x2: photo(spec: "fill-100x100") {
    id
    url
  }
  project {
    id
    title
    meta {
      slug
    }
  }
}

query TelegramChats {
  telegramChats {
    ...TelegramChat
  }
}

mutation CreateTelegramChat($input: CreateTelegramChatInput!) {
  result: createTelegramChat(input: $input) {
    ...TelegramChat
    ...ValidationError
    ...GenericError
  }
}

mutation DeleteTelegramChat($id: ID!) {
  result: deleteTelegramChat(id: $id) {
    ... on BasicResult {
      ok
    }
  }
}

mutation RefreshTelegramChatData($id: ID!) {
  result: refreshTelegramChatData(id: $id) {
    ...TelegramChat
  }
}
