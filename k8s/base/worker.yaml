apiVersion: apps/v1
kind: Deployment
metadata:
  name: kocherga-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kocherga-worker
  template:
    metadata:
      labels:
        app: kocherga-worker
    spec:
      containers:
        - name: default
          imagePullPolicy: Always
          image: registry.gitlab.com/kocherga/core/backend
          command: ["./scripts/worker.py"]
          volumeMounts:
            - name: secrets
              mountPath: "/code/kocherga/django/settings/secrets.py"
              subPath: "secrets.py"
              readOnly: true
            - name: configs
              mountPath: "/code/kocherga/django/settings/main.py"
              subPath: "main.py"
              readOnly: true
          env:
            - name: DJANGO_SETTINGS_MODULE
              value: kocherga.django.settings.main
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kocherga
                  key: db_password
      volumes:
        - name: secrets
          secret:
            secretName: kocherga
        - name: configs
          configMap:
            name: kocherga
