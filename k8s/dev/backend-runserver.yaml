apiVersion: apps/v1
kind: Deployment
metadata:
  name: kocherga-backend
spec:
  template:
    spec:
      containers:
      - name: http-server
        command: ["./manage.py", "runserver", "0.0.0.0:80"]
